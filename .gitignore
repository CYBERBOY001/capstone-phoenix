# --- Secrets & state: NEVER commit. The grader runs `git log -p` looking for these. ---
*.tfstate
*.tfstate.*
.terraform/
*.tfvars
!*.tfvars.example
crash.log
kubeseal-0.38.4-linux-amd64.tar.gz
kubeseal

# kubeconfig & cluster joins
kubeconfig
*kubeconfig*
k3s.yaml
node-token
*.token
secret.yaml

# secrets / env
.env
.env.*
!.env.example
!*secret*.example.yaml
*.pem
*.key
id_rsa*

# argo / helm local
charts/*.tgz

# os / editor
.DS_Store
*.swp
.idea/
.vscode/
